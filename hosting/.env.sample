# Supabase (buyunrepped website project — secret key, server only)
SUPABASE_URL=https://gxdqsonpcngzjiugfhzy.supabase.co
SUPABASE_SECRET_KEY=sb_secret_your_secret_key_here

# Spam guard (required in production — signed math CAPTCHA + honeypot)
SPAM_GUARD_SECRET=your_random_secret_at_least_32_chars

# Slack incoming webhook URL for contact form submissions
SLACK_CONTACT_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# Optional: separate Slack channel for waitlist signups (falls back to SLACK_CONTACT_WEBHOOK_URL)
# SLACK_WAITLIST_WEBHOOK_URL=https://hooks.slack.com/services/your/waitlist/webhook/url

# SendGrid (welcome email from Kayla on opt-in)
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=kayla@buyunrepped.com
SENDGRID_FROM_NAME=Kayla Brown

# Calendly webhook signing key (for /api/calendly-webhook)
# CALENDLY_WEBHOOK_SIGNING_KEY=your_calendly_signing_key

# Primary app URL for "Start in the app" CTAs
NEXT_PUBLIC_APP_URL=https://app.buyunrepped.com

# Optional: force launch tier fixture instead of live API (dev/preview)
# ROLLOUT_TIERS_USE_FIXTURE=1
